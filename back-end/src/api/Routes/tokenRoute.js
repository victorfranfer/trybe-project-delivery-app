// const { Router } = require('express');
// const { tokenController } = require('../Controller/tokenController');

// const router = Router();

// router.post('/', (req, res) => tokenController(req, res));

// module.exports = router;